<Window x:Class="Firewall.PasswordInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему" Height="220" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#2D2D30">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Для доступа к приложению требуется пароль" FontSize="14" Foreground="White" Margin="0,0,0,15"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Background="#3E3E42" Foreground="White" FontSize="14" Height="30" Margin="0,0,0,10"/>
        <TextBlock x:Name="ErrorText" Grid.Row="2" Text="" Foreground="Red" FontSize="12" Margin="0,0,0,15" Visibility="Collapsed"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="0,0,10,0" Click="Ok_Click" IsDefault="True"/>
            <Button Content="Отмена" Width="80" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>